import React from 'react';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    container: {
         flex:1,
         backgroundColor:'#b3fff7',
    },
    container_logo:{
       flex:1,
       alignItems:'center',
       justifyContent:'center',
    },
    container_body:{
       flex:1,
    }
}
);